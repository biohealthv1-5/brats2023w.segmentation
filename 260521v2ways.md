# 🚀 260521 v2 — 프로젝트 완성도를 한 단계 끌어올릴 SOTA 통합 개선안

> **작성일**: 2026-05-21
> **베이스라인**: `260521v1result.md` (Day 1 ~ Day 6 진행 현황)
> **출발점**: T2-FLAIR Multi-Task (F1 93.91 / IoU 0.706) + 진행 중인 T1ce+FLAIR Multi-Modal Multi-Task
> **본 문서의 의미**: `revise_analysis.md`의 Day 6 멀티모달 전환을 **유지·확장**하면서, 학부 단일 RTX 4070 Laptop 8GB 환경에서 **현실적으로 적용 가능한 SOTA 기법**을 4개 트랙(① 아키텍처, ② 학습 신호/Loss, ③ 데이터/입력, ④ 평가·신뢰성)으로 재구성하고, **순수 이진 분류에 머무르지 않는 grading/multi-task 확장 방향**까지 제안.

---

## 0. Executive Summary

### 0.1 이 문서가 기존 v1·revise_analysis와 다른 점

| 비교점 | `revise_analysis.md` (Day 6 전환 분석) | **`260521v2ways.md` (본 문서)** |
|------|---|---|
| 범위 | T1ce+FLAIR 멀티모달 전환에 집중 | **멀티모달 + SOTA 아키텍처 + Loss SOTA + Grading 확장** |
| Grading | 미고려 | **WT/TC/ET 3-region grading + Tumor-type(GLI/MEN/PED) 다중 분류 명시** |
| 모델 선택지 | ResNet-18 backbone 유지 | **ResNet-18 / MedNeXt-Small / SegFormer-B0 / DynUNet 단계적 업그레이드 로드맵** |
| Loss | Tversky 추가 권장 | **Tversky → Compound (Focal-Tversky + Boundary) → Region-aware (BraTS WT/TC/ET) 3단 진화** |
| MTL 가중 | 수동 α=1, β=0.5 | **GradNorm / Uncertainty Weighting (Kendall 2018) / PCGrad — SOTA 가중 자동화** |
| 사전학습 | ImageNet only | **ImageNet → SimMIM/MAE BraTS self-supervised → MedSAM 거리 활용** |
| 신뢰성 | Temperature Scaling 1개 | **Temperature + Deep Ensembles + Conformal Prediction (uncertainty quantification)** |

### 0.2 한 줄 결론

> **"v1까지가 *Shortcut Learning을 진단하고 Multi-Task로 극복하는 단계*였다면, v2는 *멀티모달을 활용해 grading 까지 확장 가능한 신뢰성 있는 분류·세분화·등급 통합 시스템*으로 완성하는 단계다."**

### 0.3 우선순위 한눈에 보기 (난이도·효과·소요시간)

| 순위 | 개선안 | 트랙 | 난이도 | 효과(F1) | 효과(IoU) | 추가 시간 | 우리 흐름 적합도 |
|:---:|---|---|:---:|:---:|:---:|:---:|:---:|
| 🥇 1 | **Tversky/Focal-Tversky Loss (FN-aware)** | Loss | ⭐ | +0.5~1.0%p | +0.03 | 0h | ★★★★★ |
| 🥇 2 | **Multi-Modal (T1ce+FLAIR+diff)** | 입력 | ⭐⭐ | +1.5~2.5%p | +0.07 | 4~6h | ★★★★★ |
| 🥇 3 | **GradNorm / Uncertainty Weighting** | 학습 | ⭐⭐ | +0.3~0.5%p | +0.02 | 1h 코드 | ★★★★★ |
| 🥈 4 | **WT/TC/ET 3-region multi-class seg** | 출력 (Grading) | ⭐⭐ | (분류 동등) | +0.04 | 1~2h 코드 + 학습 | ★★★★★ |
| 🥈 5 | **Tumor-type 다중 분류 (GLI/MEN/PED)** | 출력 (Grading) | ⭐⭐⭐ | (별도 head) | — | 데이터 준비 4h | ★★★★ |
| 🥈 6 | **Deep Supervision (decoder 중간 출력)** | 아키텍처 | ⭐⭐ | +0.3~0.5%p | +0.03 | 1h | ★★★★ |
| 🥈 7 | **TTA + Temperature Scaling + SWA** | 신뢰성 | ⭐ | +0.2~0.5%p | — | 1h | ★★★★ |
| 🥉 8 | **MedNeXt-Small backbone 교체** | 아키텍처 | ⭐⭐⭐ | +1.0~1.5%p | +0.04 | 학습 6~8h | ★★★ |
| 🥉 9 | **2.5D 입력 (z-1, z, z+1)** | 입력 | ⭐⭐ | +0.5~1.0%p | +0.03 | 2h + 학습 1.5배 | ★★★ |
| 🥉 10 | **Deep Ensemble (3-seed)** | 신뢰성 | ⭐ | +0.5~1.0%p | +0.02 | 학습 3배 | ★★★ |
| 🔬 11 | **Self-supervised pretraining (SimMIM)** | 사전학습 | ⭐⭐⭐⭐ | +0.5~1.5%p | +0.04 | 10h+ | ★★ |
| 🔬 12 | **MedSAM pseudo-label로 weak supervision 확장** | 사전학습 | ⭐⭐⭐⭐ | — | +0.05 | 8h+ | ★★ |
| 🔬 13 | **Conformal Prediction (uncertainty)** | 신뢰성 | ⭐⭐⭐ | 보장 커버리지 | — | 2h | ★★★★ |
| 🔬 14 | **Tumor-burden Regression head** | 출력 (Grading) | ⭐⭐ | (보조) | — | 1h | ★★★ |

> **🥇 = 즉시 적용 권장 (Day 6 ~ Day 7)**, **🥈 = Day 7~8**, **🥉 = Day 8~9**, **🔬 = 학술 연구 확장**.

---

## 1. 우리 프로젝트 흐름의 핵심 진단 (왜 이 SOTA들인가)

### 1.1 5단계 여정에서 본 "다음 한 걸음"의 조건

```
Day 2 Whole   → 수치 (Acc 94, F1 94)        | 종양을 안 봤다
Day 3 Grad-CAM → 진단 (IoU 0.145)            | shortcut 폭로
Day 4 Patch   → 시도 (IoU 0.128, passive)    | 부족
Day 5 MTL     → 성공 (IoU 0.706, active)     | 종양을 보게 됨
Day 6 MM-MTL  → 입력 표현 (T1ce+FLAIR)       | 진행 중
─────────────────────────────────────────────
Day 7+ (본 문서) → ❶ Loss·MTL 가중 자동화
                ❷ Grading 확장 (WT/TC/ET, tumor-type)
                ❸ 신뢰성·일반화 (TTA·Calibration·Ensemble·Conformal)
                ❹ 선택적 아키텍처 SOTA (MedNeXt·SegFormer)
```

### 1.2 다음 단계가 만족해야 할 5가지 제약

| 제약 | 의미 | 충족 조건 |
|------|------|----------|
| **연속성** | Day 5 MTL의 "active supervision" 철학 유지 | 분류 head는 반드시 존재, MTL은 깨지 않음 |
| **자원** | RTX 4070 8GB / 5~6h 학습 / 디스크 ~10GB 한도 | 3D / Foundation full fine-tune 배제 |
| **재현성** | 환자 단위 875/188/188 split 유지 | 추가 데이터(MEN/PED)는 보조 task로만 |
| **해석성** | shortcut 진단 + MTL 5배 향상 스토리 유지 | XAI는 segmentation 출력 + 추가 보조 |
| **학부 범위** | "Naive→진단→시도→해결" 서사로 발표 가능 | 한 줄 인과 설명이 가능한 SOTA만 채택 |

이 5개 제약을 통과한 것이 위 **🥇/🥈/🥉/🔬 표의 14가지 개선안**이다.

---

## 2. 트랙 ① — 아키텍처: 점진적 SOTA 업그레이드

### 2.1 백본 교체 로드맵 (3단 진화)

```
Stage A (현재 Day 5/6): ResNet-18 + UNet-style decoder    ─── 14M params, 5h 학습
   ↓ (Day 7+)
Stage B: ResNet-18 + Deep Supervision + GradNorm           ─── 14M params, 5h 학습 (★ 권장 시작점)
   ↓ (선택, Day 8+)
Stage C-1: MedNeXt-Small + Multi-Task                      ─── 17M params, 6~8h 학습
Stage C-2: SegFormer-B0 + Cls head (Transformer SOTA)     ─── 14M params, 6~8h 학습
   ↓ (Ablation only)
Stage D: DynUNet (nnU-Net 2D auto-config)                  ─── MONAI 한 줄, 비교용
```

### 2.2 Stage B — Deep Supervision (즉시 권장, +0 추가 학습 비용)

**아이디어**: 디코더 중간 단계 (up2, up3, up4) 출력에도 segmentation head를 달아 **다중 해상도 supervision**.

```python
# step17_multitask_model.py 수정 (최소 변경)
class MultiTaskBrainNet(nn.Module):
    def __init__(...):
        # 기존 + 추가
        self.aux_head_d2 = nn.Conv2d(decoder_channels[1], 1, 1)   # 28x28 -> upsample to 224
        self.aux_head_d3 = nn.Conv2d(decoder_channels[2], 1, 1)   # 56x56 -> upsample
        self.aux_head_d4 = nn.Conv2d(decoder_channels[3], 1, 1)   # 112x112 -> upsample

    def forward(self, x):
        # ... 기존
        seg_main = self.seg_head(self.final_up(d1))     # 224x224
        seg_aux2 = F.interpolate(self.aux_head_d2(d3), size=224, mode='bilinear')
        seg_aux3 = F.interpolate(self.aux_head_d3(d2), size=224, mode='bilinear')
        seg_aux4 = F.interpolate(self.aux_head_d4(d1), size=224, mode='bilinear')
        return cls_out, seg_main, [seg_aux2, seg_aux3, seg_aux4]

# Loss
L_seg = L(main) + 0.5*L(aux4) + 0.25*L(aux3) + 0.125*L(aux2)
```

**효과**:
- 작은 종양 feature가 깊은 layer까지 살아남도록 강제 (3wayanalysis §6 시사점 6 — 모든 모델 공통 약점에 직격).
- **추가 파라미터 < 3K, VRAM 증가 거의 없음**.
- nnU-Net / SwinUNETR 등 BraTS 상위권 솔루션의 표준 트릭.

### 2.3 Stage C-1 — MedNeXt (MICCAI 2023, 가장 최근 BraTS 친화 SOTA)

> "ConvNeXt를 의료영상에 적합하도록 재설계 + Resampling 기반의 depth/width 스케일 - 2D/3D 모두 BraTS WT 0.93 달성."

| 항목 | MedNeXt-Small (2D) vs 현재 ResNet-18 |
|---|---|
| 파라미터 | 17M vs 11M (decoder 포함 기준 17M vs 14M) |
| 입력 | 224×224 동일 |
| Inductive bias | Depthwise + LayerNorm + GELU = 종양 모양에 robust |
| 학습 시간 | ~6~8h (Day 6 MM 학습 후 1 epoch 더 늘리는 수준) |
| 권장 사용 | **Ablation 비교 베이스라인** (우리 ResNet-18 vs MedNeXt) |

**구현**: 공식 코드 `MIC-DKFZ/MedNeXt` (PyTorch, MONAI integrated) — 2D 옵션 있음. 우리 `MultiTaskBrainNet`의 encoder만 갈아 끼우면 됨 (Day 7~8).

### 2.4 Stage C-2 — SegFormer-B0 (Transformer 경량 SOTA)

> "Hierarchical Transformer encoder + lightweight all-MLP decoder. ADE20K SOTA, BraTS에도 직접 이식 가능."

| 장점 | 단점 |
|---|---|
| **광역 context**: 자기-주의로 종양 좌우 대칭/먼 위치 정보 통합 | 데이터 적으면 ResNet-18에 패배 가능 |
| Encoder 14M, 우리 한계 내 | ImageNet pretrain 효과는 같지만 의료 적합도는 ConvNet보다 약함 |
| Hugging Face `transformers` 한 줄 로드 | Multi-Task로 개조 필요 (cls head 추가) |

**권장**: Stage C-1과 둘 중 하나만. 학부 발표용으로는 MedNeXt가 BraTS 정합성 더 높다.

### 2.5 Stage D — DynUNet (MONAI nnU-Net 2D)

> 학술적 베이스라인 비교용 1개. `from monai.networks.nets import DynUNet` 한 줄. 우리 데이터로더만 맞추면 됨. **Day 9 이후 ablation table 채우기 용도**.

### 2.6 한 줄 결론 (트랙 ①)

> **"Day 7은 Stage B(Deep Supervision)로 무료 SOTA를 얻고, Day 8에서 MedNeXt 1회 ablation으로 학술적 보완. Transformer는 선택 사항."**

---

## 3. 트랙 ② — 학습 신호 / Loss: SOTA 진화 3단

### 3.1 Loss 진화 사다리

```
Day 5 현재: BCE(cls) + (Dice + BCE)(seg), β=0.5 고정
   ↓
Day 7 ① Tversky/Focal-Tversky (FN 가중)             ← revise §3.1 권장
   ↓
Day 7 ② Compound Loss = Focal-Tversky + Boundary    ← 소종양 + 경계 동시 공략
   ↓
Day 8 ③ Region-aware Loss (BraTS WT/TC/ET 분리)     ← grading 확장과 연동
   ↓
Day 8 ④ MTL 가중 자동화: GradNorm / Uncertainty / PCGrad
```

### 3.2 ① Focal-Tversky Loss (즉시 적용, 추가 학습 비용 0)

```python
class FocalTverskyLoss(nn.Module):
    def __init__(self, alpha=0.7, beta=0.3, gamma=4/3, smooth=1.0):
        super().__init__()
        self.alpha, self.beta, self.gamma, self.smooth = alpha, beta, gamma, smooth
    def forward(self, pred, target):
        p = torch.sigmoid(pred).view(-1); t = target.view(-1)
        TP = (p*t).sum()
        FN = ((1-p)*t).sum()       # α에 가중치 ← 우리는 0.7 (FN penalty 강화)
        FP = (p*(1-t)).sum()       # β=0.3
        tversky = (TP + self.smooth) / (TP + self.alpha*FN + self.beta*FP + self.smooth)
        return (1 - tversky) ** self.gamma   # focal modulation
```

**효과**: FN 1,136 → **700~900** (revise_analysis §1.3과 동일 추정). `step18_multitask_train.py`의 `DiceLoss` 클래스 한 곳만 교체하면 됨.

### 3.3 ② Compound Loss (Focal-Tversky + Boundary)

> "BraTS Challenge 상위권 (2020~2023) 공통: 영역(Tversky)+경계(Boundary) 결합."

```python
class CompoundSegLoss(nn.Module):
    def __init__(self, w_tversky=1.0, w_bce=0.5, w_boundary=0.3):
        super().__init__()
        self.tv = FocalTverskyLoss(alpha=0.7, beta=0.3)
        self.bce = nn.BCEWithLogitsLoss()
        self.w_tv, self.w_bce, self.w_b = w_tversky, w_bce, w_boundary
    def forward(self, pred, target):
        L_tv = self.tv(pred, target)
        L_bce = self.bce(pred, target)
        L_b = boundary_loss(pred, target)    # signed distance map 기반
        return self.w_tv*L_tv + self.w_bce*L_bce + self.w_b*L_b
```

`boundary_loss`는 Kervadec et al. (MIDL 2019) 구현 — 정답 마스크의 signed distance map을 사전 계산해두면 학습 시 비용 거의 0.

**예상 효과**: IoU 0.706 → **0.74~0.78** (Day 6 멀티모달과 시너지).

### 3.4 ③ Region-aware Loss (WT / TC / ET 분리) — Grading 연동의 첫걸음

현재 우리 segmentation 라벨은 "tumor any pixel = 1" (WT 기반 이진화).
BraTS 공식 평가 지표는 **WT / TC / ET 3-region**이다 (revise §1.1).

**제안**: 우리 segmentation head를 **3채널 출력 (WT, TC, ET)**으로 확장.

```python
# step17_multitask_model.py
self.seg_head = nn.Conv2d(decoder_channels[3], 3, kernel_size=1)   # 1ch -> 3ch
# Loss
L_seg = Σ_{r in {WT,TC,ET}} (Focal-Tversky + BCE)(pred[:, r], gt[:, r])
```

**라벨 생성** (`step1_preprocess.py`의 seg_data 처리):
- WT = (seg > 0).astype(float)
- TC = ((seg == 1) | (seg == 3)).astype(float)   # NCR + ET
- ET = (seg == 3).astype(float)

**이게 왜 grading인가**:
- 우리 분류 head는 여전히 "tumor 유무" (WT 기반).
- 하지만 segmentation head가 **3-region을 예측** → 출력은 "이 슬라이스에 종양 있음 + TC 영역 + ET 영역"이라는 임상적으로 더 풍부한 정보.
- 차후 단계: **"슬라이스 분류"를 multi-label** (WT 유무 / TC 유무 / ET 유무) 또는 multi-class (등급 1/2/3 — ET가 차지하는 비율로) 로 자연스럽게 확장 가능.

**BraTS 공식 평가축과 정렬되므로 SOTA 비교가 의미 있어진다** (3wayanalysis §8.3 우선순위 5 — "Multi-Class 확장 (NCR/ED/ET)"의 직접 구현).

### 3.5 ④ MTL 가중 자동화 — α, β를 손으로 정하지 말자

| 기법 | 설명 | 우리 적용 난이도 |
|------|------|:----:|
| **Uncertainty Weighting** (Kendall et al. CVPR 2018) | 각 task에 학습 가능 log-variance σ — `L = Σ exp(-s_i) L_i + s_i` | ⭐⭐ (5줄 추가) |
| **GradNorm** (Chen et al. ICML 2018) | 각 task의 gradient norm 균형화 | ⭐⭐⭐ |
| **PCGrad** (Yu et al. NeurIPS 2020) | 충돌하는 gradient를 직교 사영 | ⭐⭐⭐ |
| **MGDA** (Sener & Koltun NeurIPS 2018) | Multi-objective Pareto 최적해 | ⭐⭐⭐⭐ |

**가장 가성비 좋은 것: Uncertainty Weighting (5줄)**

```python
class MultiTaskLoss(nn.Module):
    def __init__(self):
        super().__init__()
        self.log_var_cls = nn.Parameter(torch.zeros(1))
        self.log_var_seg = nn.Parameter(torch.zeros(1))
    def forward(self, cls_pred, cls_y, seg_pred, seg_y):
        L_cls = F.binary_cross_entropy_with_logits(cls_pred, cls_y)
        L_seg = compound_seg_loss(seg_pred, seg_y)
        precision_c = torch.exp(-self.log_var_cls)
        precision_s = torch.exp(-self.log_var_seg)
        loss = precision_c*L_cls + 0.5*self.log_var_cls \
             + precision_s*L_seg + 0.5*self.log_var_seg
        return loss, L_cls.detach(), L_seg.detach()
```

**효과**:
- `revise §1.3`에서 α=1.0, β=0.5 → β를 0.3~1.0 그리드 서치로 튜닝하라고 했는데, 이제 **그리드 서치 자체가 불필요** (모델이 학습 중 자동 조절).
- 발표 시 "MTL SOTA(Kendall et al. 2018) 적용"으로 학술적 정합성도 ↑.

### 3.6 한 줄 결론 (트랙 ②)

> **"Day 7에서 Focal-Tversky + Boundary + Uncertainty Weighting 3종 패키지를 한 번에 도입하면, 손으로 정한 모든 하이퍼파라미터(β=0.5, threshold=0.3847)가 자동 보정되고 FN/IoU 동시 개선이 예상된다."**

---

## 4. 트랙 ③ — 데이터·입력 표현 강화

### 4.1 Multi-Modal (revise_analysis §1~§2 — 본 트랙의 핵심)

이미 `revise_analysis.md`에서 충분히 분석. v2에서 추가 권장:

**v2 보강 ①**: 3채널 구성을 `[T1ce, FLAIR, |T1ce - FLAIR|]` 이외에도 **`[T1ce, FLAIR, T1ce/FLAIR ratio]`** 또는 **`[T1ce*FLAIR, T1ce, FLAIR]`**의 채널 ablation도 함께. 어느 게 종양 강조에 최적인지 발표 시 작은 ablation 슬라이드 한 장 가능.

**v2 보강 ②**: 모달리티 누락 시뮬레이션 — Day 6 학습 모델을 **T1ce만**, **FLAIR만**으로 추론 (3채널 중 2개를 평균 채널로 대체) → "모달리티 ablation" 곡선. 실제 임상에서 한 모달이 빠질 때 강건성 보고.

### 4.2 2.5D 입력 (z-1, z, z+1) — revise §3.4 보강

| 구성 | 채널 | 효과 |
|------|------|------|
| 단순 | [FLAIR_z-1, FLAIR_z, FLAIR_z+1] | z-축 맥락 |
| 멀티모달+2.5D | [T1ce_z, FLAIR_z, T1ce_z-1, FLAIR_z-1, T1ce_z+1, FLAIR_z+1] = 6ch | z + modal 동시 |

**6채널 입력 시 conv1 가중치 처리**: ImageNet 3ch → mean→repeat(2) 또는 PCA 압축. 권장: **2.5D는 2ch 멀티모달 위에 z 인접 차분 채널만 1~2개 추가** (총 4채널).

### 4.3 SOTA Augmentation

| 기법 | 의료영상 적합도 | 우리 적용 |
|------|:---:|---|
| **Random Affine + Elastic Deformation** | ★★★★★ | 표준 (MONAI `RandAffine`, `Rand2DElastic`) |
| **Intensity Jitter / Bias Field** | ★★★★ | `RandGibbsNoise`, `RandBiasField` |
| **TumorCP / TumorMix** (Yang et al. 2022 MICCAI) | ★★★★★ | 종양 영역을 다른 슬라이스에 paste — **소종양 oversampling 효과** |
| **CutMix / Mixup** | ★★★ | 보조 regularizer (cls head에만 적용) |
| **RandStainNA** | ★ | MRI에는 부적합 (조직 염색이 아님) |

> **권장**: MONAI Transforms로 표준 5종(`RandAffined`, `Rand2DElasticd`, `RandGibbsNoised`, `RandBiasFieldd`, `RandFlipd`) + 종양 슬라이스에 **TumorCP** (소종양 FN 직격).

### 4.4 입력 해상도 (revise §3.2 보강)

| 해상도 | Batch (8GB) | VRAM | 권장 |
|:---:|:---:|:---:|:---:|
| 224 (현재) | 16 | 안전 | 베이스라인 |
| 320 | 8 + grad-accum 2 | 빠듯 | ★ 권장 (소종양 보존) |
| 384 | 4 + grad-accum 4 | 위험 | 최종 발표용만 |

### 4.5 클래스 균형 (revise §3.3 보강)

WeightedRandomSampler + **소종양 슬라이스에 3배 가중** (이미 revise에 있음) + 본 v2 추가:
- **Curriculum Learning**: epoch 1~3은 균형 샘플링 → epoch 4~ 은 소종양 가중 점진 증가.

### 4.6 한 줄 결론 (트랙 ③)

> **"멀티모달 + 2.5D(4ch) + 320 해상도 + TumorCP가 결합되면, '입력 표현 한계' (3wayanalysis §6 시사점 6)를 정면 돌파하는 가장 강력한 패키지가 된다."**

---

## 5. 트랙 ④ — 평가·신뢰성·일반화 (SOTA Calibration / Uncertainty)

### 5.1 Test-Time Augmentation (TTA)

```python
# 추론 시
outputs = []
for tf in [identity, hflip, vflip, rot90]:
    out_cls, out_seg = model(tf(input))
    out_seg = tf_inv(out_seg)
    outputs.append((out_cls, out_seg))
cls_mean = mean([o[0] for o in outputs])
seg_mean = mean([o[1] for o in outputs])
```

**효과**: F1 +0.2~0.5%p, IoU +0.01~0.02. 학습 비용 0. **현 발표 직전이라도 적용 가능**.

### 5.2 Stochastic Weight Averaging (SWA, Izmailov et al. UAI 2018)

```python
from torch.optim.swa_utils import AveragedModel, SWALR
swa_model = AveragedModel(model)
# 학습 후반부(예: Phase 2의 마지막 5 epoch)에 swa_model.update_parameters(model)
# 평가 직전 torch.optim.swa_utils.update_bn(loader, swa_model)
```

**효과**: 일반화 +0.3~0.7%p, **추가 학습 시간 거의 0**. Phase 2 마지막 5 epoch에 SWA를 켜면 그만.

### 5.3 Temperature Scaling (revise §3.6 보강)

```python
# Validation set에서 scalar T 학습
T = nn.Parameter(torch.ones(1))
optimizer = LBFGS([T], lr=0.01, max_iter=50)
def closure():
    optimizer.zero_grad()
    loss = nn.CrossEntropyLoss()(logits/T, labels)
    loss.backward(); return loss
optimizer.step(closure)
# 추론 시 logits/T 사용
```

**효과**: Multi-Task의 optimal threshold 0.3847이 **0.5 부근으로 회귀** → 임상 운영 직관성 ↑.

### 5.4 Deep Ensembles (3-seed) — 가장 강력한 uncertainty quantification

```python
# seed 42, 1024, 7777로 3회 학습
for seed in [42, 1024, 7777]:
    train_with_seed(seed) -> save model_seed.pth
# 추론: 3개 모델 평균
```

| 항목 | 단일 모델 | Deep Ensemble (3) |
|---|:---:|:---:|
| F1 | 95~97% (예상) | **+0.5~1.0%p** |
| IoU | 0.74~0.78 | +0.01~0.02 |
| Uncertainty | scalar 확률 | **모델 간 분산** (epistemic) |
| 학습 시간 | 5h | 15h (병렬 가능) |

**학부 환경 트릭**: 3-seed 학습이 부담스러우면 **Snapshot Ensemble** (cyclic LR로 한 번 학습에 3 체크포인트 저장) — 1회 학습으로 ensemble 효과.

### 5.5 Conformal Prediction — 보장된 커버리지

> "MAPIE, conformal-prediction 라이브러리로 90% 보장 신뢰구간 제공."

```python
# Validation set으로 calibration
from mapie.classification import MapieClassifier
mapie = MapieClassifier(estimator=our_model_wrapper, cv='prefit')
mapie.fit(X_val, y_val)
pred_sets = mapie.predict(X_test, alpha=0.1)   # 90% coverage
```

**효과**: 각 슬라이스에 대해 "이 슬라이스는 종양 있음 확신 (set={1})" / "불확실 (set={0,1})" 출력. 의료 AI 임상 적용에서 가장 중요한 SOTA 신뢰성 기법 (`Reyes et al. 2020 Radiology AI` 참고).

### 5.6 OOD / 강건성 (revise §5.2 약점 — 분포외 강건성)

- BraTS는 19개 기관에서 수집된 멀티-사이트 데이터셋. **환자 코호트별 성능 분해 (HGG vs LGG가 있던 BraTS 2017 매핑 사용)** → 발표 시 "사이트별 강건성" 분석 추가 가능.
- 또는 **MEN(수막종) / PED(소아) 데이터로 추론만** 수행하여 외부 강건성 보고.

### 5.7 한 줄 결론 (트랙 ④)

> **"TTA + SWA + Temperature Scaling 3종은 추가 학습 시간 0~10% 증가로 F1 +0.5~1.0%p / 임상 친화도 대폭 향상. Deep Ensemble + Conformal까지 가면 '학부 프로젝트' 수준을 넘어 의료 AI 신뢰성 SOTA를 모두 다루게 된다."**

---

## 6. Grading 확장 — 순수 이진을 넘어서 (사용자 핵심 요구사항)

### 6.1 우리가 갈 수 있는 4가지 grading 방향

> **"왜 grading인가"**: v1까지는 슬라이스 단위 이진 분류였지만, 임상 가치를 한 단계 높이려면 **종양 유무 → 종양의 종류·강도·구성**을 제공해야 한다. 다행히 BraTS 데이터에는 이를 위한 라벨이 **이미 존재**한다 (NCR/ED/ET + GLI/MEN/PED 3 코호트).

#### 방향 ① WT/TC/ET 3-region multi-label seg (§3.4 참조 — 가장 권장)

- 분류 head: 그대로 (tumor 유무).
- Seg head: 1ch → 3ch (WT, TC, ET 각각).
- **그 자체로 BraTS 공식 평가축과 정렬** → SOTA 직접 비교 가능.

#### 방향 ② Slice-level multi-label (tumor 유무 + TC 유무 + ET 유무)

- Cls head: 1ch → 3ch (sigmoid each).
- 한 슬라이스가 "WT=1, TC=1, ET=0" (즉 종양 있고 코어도 있지만 활성은 없음) 같은 의학적으로 풍부한 출력.
- 추가 학습 비용 거의 0.

#### 방향 ③ Tumor-type 다중 분류 (GLI / MEN / PED) — 데이터 추가 필요

- `data/BraTS-MEN`, `data/BraTS-PED` 폴더가 **이미 있음** (확인 완료).
- 환자 단위 라벨 추가:
  ```python
  patient_type = "GLI" if "GLI" in patient_path else "MEN" if "MEN" in patient_path else "PED"
  ```
- Cls head: 1 (binary) + 3 (type softmax) = 4-dim. 또는 2-stage (tumor 유무 → type).
- **혹은**: GLI 단독 학습 후 MEN/PED는 OOD 평가용으로 → 강건성 분석 (§5.6).

#### 방향 ④ Tumor burden / severity regression

- 추가 head: scalar regression (e.g., tumor pixel ratio in slice, or volume-aware grading).
- Loss: MSE 또는 ordinal regression (CORN — Cao et al. 2020).
- **분류 + 등급 동시**: "이 슬라이스에 종양 있고, 침범 비율 5% (mild)" 같은 출력.

### 6.2 추천 통합 시나리오 — "Day 7~9 Grading-Aware MM-MTL"

```
입력: (B, 3, 224, 224)  ← [T1ce, FLAIR, |T1ce-FLAIR|]
       (선택: 6ch 2.5D, 320 해상도)
                ↓
Shared Encoder (ResNet-18 + Deep Supervision)
                ↓
       ┌────────┬─────────┬─────────┐
       │        │         │         │
   Cls Head  Seg Head   Type Head   Burden Head
   (3-label) (3-region) (3-class)   (regression)
   WT/TC/ET   WT/TC/ET   GLI/MEN/PED  pixel_ratio
   sigmoid    sigmoid    softmax      tanh
       │        │         │         │
       └─── 4-task Multi-Task Loss ─┘
            (Uncertainty Weighting로 자동 가중)
```

| Head | Loss | 가중 (Uncertainty) | 학습 자료 |
|------|------|:---:|---|
| Cls (3-label) | BCE × 3 | auto | 기존 labels.csv 확장 |
| Seg (3-region) | Focal-Tversky + Boundary × 3 | auto | 기존 seg.nii.gz 분해 |
| Type | CE × 3-class | auto | 추가 (GLI/MEN/PED) |
| Burden | Smooth-L1 | auto | 자동 계산 (tumor_pixels / brain_pixels) |

**예상 결과 (보수적)**:
- WT F1: 95~97% (현재 baseline 유지 + 멀티모달 효과)
- TC F1: 90~93% (NCR+ET, T1ce 강조 효과 최대)
- ET F1: 87~91% (가장 어려운 영역)
- Type Acc: 85~92% (MEN/PED 데이터 양에 의존)
- Burden MAE: 1~2% pixel ratio

### 6.3 발표 메시지 (grading 확장의 학술적 가치)

> "본 프로젝트는 이진 분류에서 출발했으나, **Multi-Task 프레임워크의 강력함**을 입증하기 위해 ① 픽셀 단위 종양 영역(WT/TC/ET), ② 슬라이스 단위 종양 구성(multi-label), ③ 환자 단위 종양 종류(GLI/MEN/PED), ④ 종양 침범도(burden) 4가지 task를 **단일 encoder로 동시 학습**한다. 이는 BraTS Challenge의 공식 평가축과 정렬되며, 임상 진단의 단계별 정보 — 있나? 어디에? 어떤 종류? 얼마나 심각? — 를 한 forward pass로 제공한다."

### 6.4 한 줄 결론 (grading)

> **"이진 분류 → 3-region multi-label seg → tumor-type 다중 분류 → burden regression. 4단계 모두 추가 데이터 수집 없이 (BraTS 라벨만으로) 가능하며, 단일 encoder + 4 head 구조로 학습 비용은 1.2배 이하."**

---

## 7. SOTA 표 — 본 프로젝트와의 정렬 (revise §4 확장)

### 7.1 직접 비교 가능한 SOTA (2D · 학부 친화)

| 모델 | 발표 | 대표 성능 | 적합도 | 우리 적용 방안 |
|------|:----:|:--------:|:---:|---|
| **MedNeXt** | MICCAI 2023 | WT 0.93 | ⭐⭐⭐⭐⭐ | Stage C-1 backbone 교체 |
| **SegFormer-B0** | NeurIPS 2021 | ADE20K SOTA | ⭐⭐⭐⭐ | Transformer ablation |
| **DynUNet (nnU-Net 2D)** | Nature Methods 2021 | WT 0.91 | ⭐⭐⭐⭐⭐ | MONAI 1줄, baseline |
| **SwinUNETR-v2** | CVPR 2024 | WT 0.92 (3D) | ⭐⭐⭐ | 2D 변형 필요 |
| **UNETR++** | TMI 2023 | WT 0.91 (3D) | ⭐⭐⭐ | 2D 변형 필요 |
| **TransBTS** | MICCAI 2021 | WT 0.90 | ⭐⭐⭐ | Multi-modal CNN+Transformer hybrid |
| **HD-GLIO** | Neuro-Oncol 2019 | WT 0.91 | ⭐⭐ | 3D, 임상 적용 사례 (참고용) |
| **MedSAM** | Nature Comm 2024 | universal seg | ⭐⭐⭐ | pseudo-label / 평가 보강 |

### 7.2 분류 SOTA (참고)

| 모델 | 데이터셋 | 우리 활용 |
|------|---|---|
| **ConvNeXt-Tiny** | ImageNet 87% | encoder 교체 ablation |
| **EfficientNet-B0** | ImageNet 77% | 경량 비교 |
| **BiT-ResNet-50** | 다중 의료 | Transfer learning 비교 |
| **BiomedCLIP** | PMC-15M | feature extractor (frozen) |

### 7.3 MTL SOTA

| 기법 | 발표 | 우리 적용 (난이도) |
|------|:----:|:---:|
| Uncertainty Weighting (Kendall) | CVPR 2018 | ⭐⭐ |
| GradNorm | ICML 2018 | ⭐⭐⭐ |
| PCGrad | NeurIPS 2020 | ⭐⭐⭐ |
| MGDA | NeurIPS 2018 | ⭐⭐⭐⭐ |
| RotoGrad | ICLR 2022 | ⭐⭐⭐ |

### 7.4 Calibration / Uncertainty SOTA

| 기법 | 발표 | 우리 적용 |
|------|:----:|---|
| Temperature Scaling | ICML 2017 | ⭐⭐ (필수) |
| Deep Ensembles | NeurIPS 2017 | ⭐⭐ (3-seed) |
| MC Dropout | ICML 2016 | ⭐ (가장 쉬움) |
| **Conformal Prediction** | various | ⭐⭐⭐ (의료 AI 신뢰성 SOTA) |
| Evidential Deep Learning | NeurIPS 2018 | ⭐⭐⭐⭐ |

### 7.5 BraTS Augmentation SOTA

| 기법 | 발표 | 효과 |
|------|:----:|---|
| **TumorCP** | MICCAI 2022 | 소종양 oversampling, **+1.5%p Dice** |
| Random elastic + bias field | MICCAI 2019 표준 | 도메인 일반화 |
| GAN-based tumor synthesis (Brain-GAN) | MedIA 2020 | 데이터 부족 환경 |

---

## 8. 통합 실행 계획 — Day 6 ~ Day 10 단계별

### 8.1 Day 6 (현재 — revise_analysis 그대로 진행)

```
① step1 확장 (T1ce 추출)                                    1~2h
② step19_mm_dataset.py (멀티모달 페어)                       30m
③ step15_mm_mt_model.py: 3ch ResNet-18 + UNet decoder         1h
④ step16_mm_mt_train.py + Tversky Loss (α=0.7, β=0.3)         1h
⑤ 학습                                                       4~6h
⑥ 평가 + 4-way 비교                                          1~2h
                                          합계 ≈ 10~14h
```

### 8.2 Day 7 — "무료 SOTA 패키지" (학습 시간 거의 추가 없음)

```
🥇 (A) Deep Supervision (decoder aux head 3개)              30m 코드
🥇 (B) Focal-Tversky + Boundary Loss                        45m 코드
🥇 (C) Uncertainty Weighting (α/β 자동)                     30m 코드
🥈 (D) WT/TC/ET 3-region seg head 확장                       1h 코드 + step1 라벨 분해
🥇 (E) MONAI augmentation 5종 + TumorCP                     1h
🥈 (F) TTA + Temperature Scaling                            1h
                                          합계 코드 ≈ 5h
[학습 1회] 멀티모달 + 위 6종 패키지 동시 적용                5~6h 학습
[평가]                                                      1h
                                          합계 ≈ 12h
```

**Day 7 종료 시점 예상 성능**:
- WT F1: **95.5~97%**, WT IoU: **0.75~0.80**
- FN: **600~800** (Tversky + TumorCP 시너지)
- FP: **150~250**
- TC F1: **88~92%**, ET F1: **84~89%**

### 8.3 Day 8 — Grading 확장 + 신뢰성

```
🥈 (G) Tumor-type head 추가 (GLI/MEN/PED 데이터 정리)        2~3h
🥈 (H) Burden regression head                                1h 코드
🥉 (I) SWA + Deep Ensemble (3-seed로 학습)                  학습 3회 (병렬 가능) 15h
🔬 (J) Conformal Prediction calibration                      2h
[학습]                                                      5~6h × 3 (또는 1회 SWA)
[평가 + 4 head 종합 보고서]                                  2h
                                          합계 ≈ 20h (병렬 시 8~10h)
```

### 8.4 Day 9 — 학술적 베이스라인 비교 (선택)

```
🥉 (K) MedNeXt-Small backbone 교체 + 학습                   8h
🥉 (L) MONAI DynUNet (nnU-Net 2D) 베이스라인                6h
[Ablation 통합 보고서]                                       2h
                                          합계 ≈ 16h
```

### 8.5 Day 10 — 최종 발표·문서 정리

```
- 6-way 비교 표 (Whole / Patch / MTL / MM-MTL / +Grading / MedNeXt)
- 학술 인사이트 정리: Shortcut → MTL → MM → Grading → Calibration
- 임상 가치 4단계: 분류 → 위치 → 종류 → 침범도
```

---

## 9. 완성도 제고 — "프로젝트 마무리" 체크리스트

### 9.1 학술 완성도

- [ ] 6-way ablation table (Whole / Patch / MTL / MM-MTL / MM-MTL+Grading / MedNeXt)
- [ ] 각 모델의 FN 슬라이스 시각화 (어떤 케이스를 누가 놓치는가)
- [ ] Modality ablation: T1ce-only / FLAIR-only / T1ce+FLAIR / 4-modal
- [ ] MTL Loss weighting ablation: 수동 (0.3/0.5/1.0) vs Uncertainty Weighting
- [ ] Calibration plot (reliability diagram, Brier score)
- [ ] Conformal coverage report (90%, 95% 보장)

### 9.2 임상 완성도

- [ ] ROC + PR 곡선 + 각 threshold의 운영점 (Youden, F1-max, 95% Recall)
- [ ] FP/FN의 임상 비용 모델: 정상 1,000명 / 종양 1,000명 시뮬레이션
- [ ] 4 head 출력 통합 시각화: "분류 / 위치 / 종류 / 침범도" 4-panel 슬라이스 뷰어
- [ ] 모달리티 누락 강건성 분석 (T1ce 없을 때 / FLAIR 없을 때 성능)
- [ ] OOD 강건성 (MEN/PED 데이터에 대한 zero-shot 평가)

### 9.3 코드 완성도

- [ ] `code/multimodal/` 하위 디렉토리 정리 (step15~step19 + grading head)
- [ ] `config.yaml` 또는 `Hydra` 도입으로 하이퍼파라미터 일원화
- [ ] `train_one_command.py` — 모든 실험을 단일 진입점으로
- [ ] 모델 카드 (`MODEL_CARD.md`): 학습 데이터, 라이선스, 한계, 의도된 사용처
- [ ] 재현성 보장: seed 고정, requirements.txt, 환경 변수 문서화

### 9.4 발표·문서 완성도

- [ ] **메인 슬라이드 (10페이지)**:
  1. 문제 정의 + BraTS 데이터
  2. Day 2 Whole-Slice 결과
  3. Day 3 Grad-CAM 폭로 (★ 결정적 슬라이드)
  4. Day 4 Patch 실패의 교훈
  5. Day 5 MTL 성공
  6. Day 6 멀티모달 전환
  7. Day 7 SOTA 패키지 (Deep Sup + Focal-Tversky + Uncertainty + TumorCP)
  8. Day 8 Grading 확장 (WT/TC/ET + GLI/MEN/PED)
  9. SOTA 비교 + 차별화 포지셔닝
  10. 임상 가치 + 한계 + 향후 방향
- [ ] **부록 슬라이드 (5페이지)**: ablation, calibration, conformal, 미실행 항목
- [ ] **데모**: Grad-CAM + Segmentation overlay 인터랙티브 viewer (optional)

---

## 10. 최종 비교 — v1 → v2가 만드는 차이

### 10.1 정량 (예상)

| 단계 | F1 (WT) | IoU (WT) | FN | FP | 추가 기능 |
|:----|:---:|:---:|:---:|:---:|---|
| Day 5 MTL (현재) | 93.91% | 0.706 | 1,136 | 305 | 분류 + WT seg |
| Day 6 MM-MTL (revise) | 95~97% | 0.74~0.78 | 700~900 | 150~250 | + T1ce 멀티모달 |
| **Day 7 v2 무료팩** | **96~98%** | **0.78~0.82** | **600~800** | **120~200** | + Deep Sup + Focal-Tversky + Uncertainty + TumorCP + TTA |
| **Day 8 v2 grading** | WT 97% / **TC 91% / ET 87%** | WT 0.80 / TC 0.72 / ET 0.65 | 〃 | 〃 | + 3-region + Type + Burden |
| **Day 8 v2 신뢰성** | + Deep Ensemble | + SWA | + Conformal coverage 90% | + Calibration ECE<0.02 | + uncertainty quantification |

### 10.2 정성

| 가치 | v1 (현재 발표 가능 수준) | v2 (본 문서 적용 후) |
|------|---|---|
| 학술 메시지 | "Shortcut → MTL 5배 향상 → 멀티모달" | **"+ 자동 MTL 가중 + Grading + 신뢰성 SOTA"** |
| 임상 메시지 | "위치 + 분류 동시" | **"위치 + 분류 + 종류 + 침범도 + 신뢰구간"** |
| SOTA 비교 | "차별화 (분류 친화·해석성)" | **"+ SOTA 베이스라인 직접 비교 (MedNeXt/DynUNet)"** |
| 재현성 | 단일 모델 | **+ Ensemble / SWA / Conformal** |
| 학부 발표 | 5단계 여정 | **7단계 + grading 차별점 + SOTA 정렬** |

---

## 11. 한 줄 요약 (다시)

> **"v1이 *Shortcut Learning을 찾아내고 Multi-Task Learning으로 극복한 정석 의료 AI 여정*이었다면, v2는 *그 여정을 멀티모달·grading·SOTA 신뢰성으로 확장하여 학부 프로젝트의 완성형 — 분류 + 영역 + 종류 + 침범도 + 신뢰구간을 단일 forward pass로 제공하는 임상 친화 시스템*으로 마무리하는 단계다."**

| 단계 | 모델/기법 | F1 (WT) | IoU (WT) | Grading | 신뢰성 | 종합 |
|:----:|---|:---:|:---:|:---:|:---:|:---:|
| Day 5 | MTL | 93.91 | 0.706 | ❌ | ❌ | 온전한 성공(1차) |
| Day 6 | MM-MTL (revise) | 95~97 | 0.74~0.78 | ❌ | ❌ | 입력 표현 강화 |
| **Day 7** | **+ 무료 SOTA 패키지** | **96~98** | **0.78~0.82** | ⚠️ (WT/TC/ET) | ⚠️ (TTA) | **거의 완성** |
| **Day 8** | **+ Grading + Ensemble + Conformal** | **97~98** | **0.80~0.84** | ✅ 4-task | ✅ Calibration+Conformal | **완성형** |

---

## 12. 부록 — 즉시 실행 권장 5종 코드 패치 위치

| # | 변경 위치 | 변경 내용 | 예상 line 추가 |
|:-:|---|---|:---:|
| 1 | `code/step1_preprocess.py` | T1ce 동시 추출 + WT/TC/ET seg 마스크 PNG 저장 | +60 |
| 2 | `code/multitask/step16_multitask_dataset.py` | 멀티모달 페어 + 3-channel seg 반환 | +40 |
| 3 | `code/multitask/step17_multitask_model.py` | 3ch input + 3ch seg head + decoder aux head 3개 + Uncertainty params | +50 |
| 4 | `code/multitask/step18_multitask_train.py` | FocalTverskyLoss + Boundary + MultiTaskLossUncertainty + TumorCP collate | +120 |
| 5 | `code/multitask/step19_multitask_evaluate.py` | TTA + Temperature Scaling + WT/TC/ET 분해 평가 + Conformal | +100 |

**총 코드 추가 ~370 line, 새 파일 0개.** 기존 step15~step19 구조 그대로 확장 가능.

---

## 13. 추천 라이브러리 (Day 6~8 즉시 설치)

```bash
pip install monai==1.3.0          # transforms, DynUNet, SwinUNETR
pip install segmentation-models-pytorch==0.3.3   # MedNeXt / SegFormer 등 모듈식 backbone
pip install mapie==0.8.3          # Conformal Prediction
pip install pytorch-lightning==2.2 # (선택) 학습 코드 간결화
# MedNeXt 공식 (선택)
pip install git+https://github.com/MIC-DKFZ/MedNeXt.git
```

---

## 14. 참고 논문 (v1 §C.3 보강 — SOTA 측 추가)

### 14.1 본 v2 권장 SOTA의 출처

| # | 논문 | 본 문서의 §  |
|:-:|------|:---:|
| ① | Roy et al., **MedNeXt**, MICCAI 2023 | §2.3, §7.1 |
| ② | Xie et al., **SegFormer**, NeurIPS 2021 | §2.4 |
| ③ | Hatamizadeh et al., **SwinUNETR**, BrainLes 2022 | §7.1 |
| ④ | Isensee et al., **nnU-Net**, Nature Methods 2021 | §2.5, §7.1 |
| ⑤ | Kendall et al., **Uncertainty Weighting**, CVPR 2018 | §3.5 ★ 핵심 |
| ⑥ | Chen et al., **GradNorm**, ICML 2018 | §3.5 |
| ⑦ | Yu et al., **PCGrad**, NeurIPS 2020 | §3.5 |
| ⑧ | Abraham & Khan, **Focal Tversky Loss**, ISBI 2019 | §3.2 |
| ⑨ | Kervadec et al., **Boundary Loss**, MIDL 2019 | §3.3 |
| ⑩ | Yang et al., **TumorCP**, MICCAI 2022 | §4.3 |
| ⑪ | Izmailov et al., **SWA**, UAI 2018 | §5.2 |
| ⑫ | Guo et al., **Temperature Scaling**, ICML 2017 | §5.3 |
| ⑬ | Lakshminarayanan et al., **Deep Ensembles**, NeurIPS 2017 | §5.4 |
| ⑭ | Angelopoulos & Bates, **Conformal Prediction**, FNT 2023 | §5.5 ★ |
| ⑮ | Ma et al., **MedSAM**, Nature Communications 2024 | §7.1 |

### 14.2 기존 v1 추천 논문 (유지)

`260521v1result.md` 부록 C.3 그대로 — Geirhos 2020 / DeGrave 2021 (Shortcut) / Caruana 1997 (MTL 원조) / Mlynarski 2019 (분류+세분화 혼합) — 본 프로젝트의 **스토리 핵심 근거**.

---

> 📎 **본 문서의 위치**: `biohealth_lv.1/260521v2ways.md` (v1과 같은 위치, 부모 폴더).
> 📎 **연속성**: `revise_analysis.md` (Day 6 멀티모달) → `260521v2ways.md` (Day 7~8 SOTA 통합 + Grading) → 최종 발표 자료.
> 📎 **다음 행동**: 14개 개선안 중 **🥇 표시 4개를 Day 6 학습 직후 (Day 7 시작 시) 한 번에 코드에 통합**하여 단일 학습으로 동시 적용 권장.
